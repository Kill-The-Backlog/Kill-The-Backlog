---
description: Route file structure conventions for remix-flat-routes
globs: apps/main/app/routes/**
alwaysApply: false
---

# Route File Structure

This project uses `remix-flat-routes` for file-based routing.

## Nested route segments

Use **directory nesting** (not dot notation) for multi-segment route paths:

```
# ✅ GOOD — directory nesting
$parentId/children/$childId/_route.tsx

# ❌ BAD — dot notation for segments
$parentId/children.$childId/_route.tsx
```

Both produce the same URL (`/:parentId/children/:childId`), but directory nesting is the project convention.
