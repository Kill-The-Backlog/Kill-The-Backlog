# ──────────────────────────────────────────────────────────────
# Kill The Backlog – Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and fill in the values below.
#
#   cp .env.example .env
#
# For Docker Compose, you only need the GitHub OAuth section.
# Everything else has working defaults.
# ──────────────────────────────────────────────────────────────

# ── GitHub OAuth (required) ──────────────────────────────────
# Create a GitHub OAuth App at https://github.com/settings/developers
# and set the callback URL to http://localhost:3000/auth/github/callback
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# ── Session ──────────────────────────────────────────────────
# Random string for cookie encryption. The default is fine for
# local use; set a strong random value in production.
SESSION_SECRET=local-development-secret

# ── AI card runs (recommended) ───────────────────────────────
# Without these the app works but is just a kanban board —
# AI card runs are the core feature. Both services have free tiers.
# ANTHROPIC_API_KEY=
# E2B_API_KEY=
