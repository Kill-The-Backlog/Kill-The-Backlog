# ──────────────────────────────────────────────────────────────
# Kill The Backlog – Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and fill in the values below.
#
#   cp .env.example .env
# ──────────────────────────────────────────────────────────────

# - GitHub OAuth -
# Create a GitHub OAuth App at https://github.com/settings/developers.
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# - Required AI provider -
# Anthropic is required for session titling and fallback agent usage.
ANTHROPIC_API_KEY=

# - Optional AI provider fallback -
# OpenAI is only needed if you want a server-level fallback key. User-saved
# provider keys take precedence for new sessions.
OPENAI_API_KEY=

# Encrypts user-saved provider API keys before writing them to the database.
# Generate a unique production value, e.g. `openssl rand -base64 32`.
PROVIDER_API_KEY_ENCRYPTION_SECRET=local-development-provider-key-secret

# - E2B sandbox -
# Agents run inside E2B cloud sandboxes. Free tier available.
E2B_API_KEY=

# Name of the E2B template to launch sandboxes from. `e2b-template-dev` is
# produced by `pnpm tools:e2b-template e2b:build:dev` (see tools/e2b-template).
E2B_TEMPLATE_NAME=e2b-template-dev

# - Session -
# Random string for cookie encryption. The default is fine for local use;
# in production generate a strong value, e.g. `openssl rand -base64 32`.
SESSION_SECRET=local-development-secret
